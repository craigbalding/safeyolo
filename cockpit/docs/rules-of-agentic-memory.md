# Persistent agentic memory

1. Update `agents.memory.md` files of the given module (within the relevant `modules of this scope`) with critical learnings.
    * Sometimes we learn important context by trial and error. When the agent/model driven session discovers particular ways of proper implementation in the context of the given API behaviors, correct parameter formats, or other implementation details through testing, immediately document them in `agents.memory.md`. This is essential for future sessions to avoid repetitive mistakes or spending tokens on resolving complicated questions already answered.

  - Extend that persistent context with implementation details
  - Update this with useful stuff learnt!)
