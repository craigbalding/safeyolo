__pycache__/
*.pyc
*.pyo
.venv/
data/
logs/
.claude/
CLAUDE.md
*.egg-info/
.pytest_cache/
.coverage
.hypothesis/

# Host-specific config (each host has its own)
docker-compose.override.yml
.env

# Large ML model files (regenerate with scripts/export_piguard_onnx_v2.py)
models/piguard-onnx/model.onnx
models/piguard-onnx/tokenizer.json

# Archive folders (old prototype code, not for public release)
_archive_prototype/
_archive_plugins/

# Guest build artifacts (kernel Image, initramfs, rootfs-base.ext4).
# Regenerate via `cd guest && ./build-all.sh`.
guest/out/

# Scratch dirs for --rootfs-script builds on macOS (staged under the
# already-mounted guest/ path so the Lima VM can read/write without a
# per-run mount change).
guest/.scratch/

# Swift build artifacts for the safeyolo-vm helper.
# Regenerate via `cd vm && make build` (or `make install`).
vm/.build/
