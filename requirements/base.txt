# Core dependencies for SafeYolo base build
# Used by: Dockerfile base stage, local development

mitmproxy>=10.0.0
httpx>=0.25.0
tenacity>=8.2.0
pyyaml>=6.0
yarl>=1.9.0
confusable-homoglyphs>=3.3.0
pydantic>=2.12.5
