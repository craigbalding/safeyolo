name: "SafeYolo CodeQL Config"

paths-ignore:
  - tests
  - cli/tests
  - "**/test_*.py"
  - "**/*_test.py"
  - "**/conftest.py"
  # Deliberately unsafe specimens used only by `semgrep test`, not runtime code.
  - tools/acceptance/semgrep-rules.py
