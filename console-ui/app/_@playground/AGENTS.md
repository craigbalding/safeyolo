# Playground folder of console-ui/app

**⚠️ CRITICAL: Never read/load/fetch files in this folder and below, except when explicitly instructed to look into a specific (named in the instruction) file** 